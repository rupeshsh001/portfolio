import React from "react";
import ContactForm from "~/components/Forms/ContactForm";

const Contacts = () => {
    return (
        <div>
            <ContactForm />
        </div>
    );
};

export default Contacts;
